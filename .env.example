# Copy to .env.local for local development. Leave empty to use the built-in
# dev mock (responses are logged to the console, nothing is saved).
VITE_RESPONSES_ENDPOINT=https://script.google.com/macros/s/XXXXXXXX/exec
